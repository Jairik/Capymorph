# Frontend

Uses phaser for the main game logic, with React (TS+Vite) as an overall framework and zustand for state management. Connects to the backend api via HTTP calls for auth (tbd), leaderboards, and questions
